import streamlit as st
from ui_utils import setup_page

setup_page("K-Means Text Clustering", "Consolidated into Machine Learning Dashboard", "🌌")

st.markdown('<div class="premium-card">', unsafe_allow_html=True)
st.header("🌌 K-Means Text Clustering Module")
st.info("ℹ️ All Machine Learning algorithms (including **K-Means Text Clustering** and **Anomaly Detection**) are consolidated into **Page 7: Machine Learning**.")
st.markdown("Navigate to **7_Machine_Learning ➔ Tab 3: K-Means Clustering** in the sidebar to run clustering analysis on your active dataset.")
st.markdown('</div>', unsafe_allow_html=True)
